{% extends ':frontend:base.html.twig' %}

{% block body %}
    <div class="ui stackable two column vertically divided grid basic segment container">
        <div class="row">
            <div class="seven wide column">
                <h3 class="ui dividing header">Reserva {{ day }}/{{ month }}/{{ year }}</h3>
                {{ form_start(bookingForm) }}
                {{ form_widget(bookingForm) }}
                {#<button type="submit" id="app_bundle_customer_type_send" name="app_bundle_customer_type[send]" class="ui positive ui button" formnovalidate><i class="add to calendar icon"></i> Reservar</button>#}
                <a class="negative ui button" href="{{ path('front_homepage') }}"><i class="remove icon"></i> Cancelar</a>
                {{ form_end(bookingForm) }}
            </div>
        </div>
    </div>
{% endblock %}
