{% extends ':frontend:base.html.twig' %}

{% block body %}
    <div class="ui vertical stripe segment">
        <div class="ui middle aligned stackable grid container">
            <div class="row">
                <div class="twelve wide column">
                    <h3>Reserva {{ day }}/{{ month }}/{{ year }}</h3>
                </div>
            </div>
        </div>
    </div>
{% endblock %}